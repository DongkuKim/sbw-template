# Example Architecture

This directory stores concrete architecture reference docs used by the worked example.

Use root `architecture/` for reusable standards and starter reference templates.
Use `example/architecture/` when you want to inspect how those references look in a completed example plan.
