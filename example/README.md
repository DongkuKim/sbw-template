# Example

Worked example docs live here in the same mirrored format as the main template:

- `features/`
- `domains/`
- `server/`
- `bff/`
- `web/`

Use this tree as a reference implementation of the Markdown contract. For new work, start from the top-level starter templates instead of editing the example directly.
